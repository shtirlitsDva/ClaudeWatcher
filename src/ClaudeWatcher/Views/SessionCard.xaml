<UserControl x:Class="ClaudeWatcher.Views.SessionCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:ClaudeWatcher.ViewModels">

    <Border x:Name="CardBorder"
            CornerRadius="6"
            Background="{StaticResource CardBackgroundBrush}"
            Padding="12"
            Margin="0,0,0,6"
            Cursor="Hand"
            MouseLeftButtonUp="Card_Click">
        <Border.Style>
            <Style TargetType="Border">
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource CardHoverBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Border.Style>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Row 0: Status + Title + Model + Time -->
            <DockPanel Grid.Row="0">
                <!-- Status indicator -->
                <Grid Width="14" Height="14" Margin="0,0,8,0" DockPanel.Dock="Left"
                      VerticalAlignment="Center">
                    <!-- Working: spinning arc -->
                    <Path x:Name="SpinnerPath"
                          Data="M 7,1 A 6,6 0 1 1 1,7"
                          Stroke="{StaticResource BlueAccentBrush}"
                          StrokeThickness="2"
                          StrokeStartLineCap="Round"
                          StrokeEndLineCap="Round"
                          RenderTransformOrigin="0.5,0.5"
                          Visibility="Collapsed">
                        <Path.RenderTransform>
                            <RotateTransform x:Name="SpinnerRotation" />
                        </Path.RenderTransform>
                    </Path>

                    <!-- Waiting: flashing yellow dot -->
                    <Ellipse x:Name="WaitingDot"
                             Width="10" Height="10"
                             Fill="{StaticResource YellowAccentBrush}"
                             Visibility="Collapsed" />

                    <!-- Error: flashing red dot -->
                    <Ellipse x:Name="ErrorDot"
                             Width="10" Height="10"
                             Fill="{StaticResource RedAccentBrush}"
                             Visibility="Collapsed" />

                    <!-- Idle: solid green dot -->
                    <Ellipse x:Name="IdleDot"
                             Width="10" Height="10"
                             Fill="{StaticResource GreenAccentBrush}"
                             Visibility="Collapsed" />
                </Grid>

                <!-- Elapsed time (right) -->
                <TextBlock DockPanel.Dock="Right"
                           Text="{Binding ElapsedTime}"
                           Foreground="{StaticResource SecondaryTextBrush}"
                           FontSize="11"
                           VerticalAlignment="Center"
                           Margin="6,0,0,0" />

                <!-- Model badge (right) -->
                <Border DockPanel.Dock="Right"
                        CornerRadius="3"
                        Background="{StaticResource SurfaceBrush}"
                        Padding="5,1"
                        VerticalAlignment="Center"
                        Visibility="{Binding ModelName, Converter={StaticResource StringToVisibility}}">
                    <TextBlock Text="{Binding ModelName}"
                               FontSize="10"
                               Foreground="{StaticResource SecondaryTextBrush}" />
                </Border>

                <!-- Title -->
                <TextBlock Text="{Binding Title}"
                           FontWeight="SemiBold"
                           FontSize="14"
                           Foreground="{StaticResource PrimaryTextBrush}"
                           TextTrimming="CharacterEllipsis"
                           VerticalAlignment="Center" />
            </DockPanel>

            <!-- Row 1: Message -->
            <TextBlock Grid.Row="1"
                       Text="{Binding DisplayMessage}"
                       Foreground="{StaticResource MutedTextBrush}"
                       FontSize="12"
                       TextWrapping="Wrap"
                       MaxHeight="40"
                       TextTrimming="CharacterEllipsis"
                       Margin="22,4,0,0" />

            <!-- Row 2: Context usage bar -->
            <Grid Grid.Row="2" Height="3" Margin="0,8,0,0">
                <Border Background="{StaticResource SurfaceBrush}" CornerRadius="1.5" />
                <Border HorizontalAlignment="Left"
                        CornerRadius="1.5"
                        Width="{Binding ContextBarWidth}"
                        Background="{Binding ContextBarBrush}" />
            </Grid>
        </Grid>
    </Border>
</UserControl>
